# scripting -> scripting_learinig GitHub backup — YAKUNLANDI (2026-07-21)

Foydalanuvchi ko'rsatmasi: `safira` github'ga ulanishi shart emas. `scripting`
shunchaki laboratoriya, keyin o'zi o'chiradi, lekin xohласа backup qilib qo'yish
uchun repo tayyorlab bergan: `ummatovfayzilllo/scripting_learinig` (bo'sh repo,
git init qilinmagan holda GitHub'da oldindan ochilgan).

## Bajarildi
- `~/Desktop/scripting` da `git init`, README.md yozildi, `variables_test.sh`
  ham qo'shildi (foydalanuvchi bergan komandalarda faqat README bor edi, lekin
  backup maqsadida asl fayl ham commit qilindi).
- Remote: `git@github.com-ummatov:ummatovfayzilllo/scripting_learinig.git`
  (MUHIM: oddiy `git@github.com:...` emas — chunki default SSH key
  `fayzillo95` accountiga ulanadi. `ummatovfayzilllo` uchun alohida SSH alias
  bor: `~/.ssh/config` dagi `Host github.com-ummatov`, key:
  `~/.ssh/id_rsa_ummatovfayzillo`.)
- Push qilindi, 2 commit: "first commit" (README) va "Add variables_test.sh".
- https://github.com/ummatovfayzilllo/scripting_learinig

## Boshqa
- `Toylar` papkasi o'chirildi (`rm -rf ~/Desktop/Toylar`) — foydalanuvchi
  allaqachon backup qilgan, papka amalda bo'sh edi.
- `safira` — tegilmadi (github kerak emas).
